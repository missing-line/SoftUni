function solve(input) {
    let count = 0;
    let countLength = 0;
    for (let i = 0; i < input.length - 1; i++) {
        let arr = input[i];
        
    }
    console.log(count);
}

solve([['test', 'yes', 'yo', 'ho'],
    ['well', 'done', 'yo', '6'],
    ['not', 'done', 'yet', '5']]
);